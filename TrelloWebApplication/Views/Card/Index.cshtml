@model IEnumerable<TrelloWebApplication.Models.Card>


@{
    ViewBag.Title = "Index";
}

<h2>Trello Cards List</h2>
<br />
<table class="table">
    <tr>
        <th>
            Trello cards' names list
        </th>


<h1>Card Trello</h1>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            Attività
        </th>

        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>

        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>



            @Html.ActionLink("Details", "Details", new { id=item.Id }) 


            @Html.DisplayFor(modelItem => item.IdList)
        </td>

        <td>
            @Html.ActionLink("Details", "Details", new { id = item.Id }) 
        </td>
    </tr>
}

</table>

@Html.ActionLink("PRINT PDF", "ExportPDF", "Report")
@Html.ActionLink("PRINT EXCEL", "ExportExcel", "Card")
